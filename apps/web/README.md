# LabelLens Web

Next.js PWA shell for the LabelLens mobile-first food search and temporary menu flow.

## Commands

```bash
npm run dev -w apps/web
npm run build -w apps/web
```

The app expects the API on `http://localhost:4000` in local development.
